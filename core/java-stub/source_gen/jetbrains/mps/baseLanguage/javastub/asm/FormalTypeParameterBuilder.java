package jetbrains.mps.baseLanguage.javastub.asm;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import java.util.List;
import java.util.ArrayList;

/**
 * Helps to construct ASMFormalTypeParameter for a class/method
 */
@GeneratedClass(nodeId = "5010111677535764961", model = "r:eafb5d8e-2952-4826-b4ad-be2b9011f598(jetbrains.mps.baseLanguage.javastub.asm)")
/*package*/ class FormalTypeParameterBuilder {
  private String myName = null;
  private ASMType myClassBound;
  private final List<ASMType> myInterfaceBounds = new ArrayList<>();
  private final List<ASMFormalTypeParameter> myResult = new ArrayList<>();

  /*package*/ void next(String name) {
    if (myName != null) {
      flush();
    }
    myName = name;
  }

  /**
   * once per formal type parameter
   */
  /*package*/ void classBound(ASMType classBound) {
    assert myClassBound == null;
    myClassBound = classBound;
  }

  /**
   * 0..n per parameter
   */
  /*package*/ void interfaceBound(ASMType ifaceBound) {
    myInterfaceBounds.add(ifaceBound);
  }

  /**
   * invoke when no more format type parameters expected
   */
  /*package*/ void complete() {
    if (myName != null) {
      flush();
    }
  }

  /*package*/ List<ASMFormalTypeParameter> result() {
    return myResult;
  }

  private void flush() {
    // null for j.l.Object is the way TypeUtil handled classBound, no idea if/how it's justified
    myResult.add(new ASMFormalTypeParameter(myName, (ASMClassType.OBJECT.equals(myClassBound) ? null : myClassBound), myInterfaceBounds));
    myName = null;
    myClassBound = null;
    myInterfaceBounds.clear();
  }
}
