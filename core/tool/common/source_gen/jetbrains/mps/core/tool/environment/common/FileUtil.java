package jetbrains.mps.core.tool.environment.common;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import java.io.File;

@GeneratedClass(node = "r:58ba176b-8a52-400c-bf27-725996471cb9(jetbrains.mps.core.tool.environment.common)/5198178184416573141", model = "r:58ba176b-8a52-400c-bf27-725996471cb9(jetbrains.mps.core.tool.environment.common)")
public final class FileUtil {
  private FileUtil() {
  }

  public static String getAbsolutePath(String path) {
    // copy of j.m.util.PathUtil.getAbsolutePath, to avoid dependency from MPS.Core/[kernel] 
    if (path.startsWith("~/") || path.startsWith("~\\")) {
      path = System.getProperty("user.home") + path.substring(1);
    }
    return new File(path).getAbsolutePath();
  }

  public static String trimPathQuotes(String path) {
    if (path == null || path.length() < 3) {
      return path;
    }
    if (StringUtil.startsWithChar(path, '"') && StringUtil.endsWithChar(path, '"')) {
      return path.substring(1, path.length() - 1);
    }
    return path;
  }
}
