package jetbrains.mps.tool.common;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import java.util.Objects;

/**
 * Idealy, path should be absolute. However, for compatibility reasons, relative paths are temporary supported.
 * In EnvironmentConfig.addPlugin(), those are converted to absolute paths.
 * todo [MM]: rewrite addPlugin() usages to absolute paths, remove compatibility code
 * 
 * @Immutable
 */
@GeneratedClass(nodeId = "496689236032862552", model = "r:067fd2c9-d009-4506-91db-a69992d65964(jetbrains.mps.tool.common)")
public class PluginData {
  public final String path;
  public final String id;

  public PluginData(String path2Plugin, String pluginId) {
    path = path2Plugin;
    id = pluginId;
  }

  @Override
  public int hashCode() {
    return Objects.hash(path, id);
  }

  @Override
  public boolean equals(Object that) {
    if (that == null || that.getClass() != this.getClass()) {
      return false;
    }
    return Objects.equals(this.path, ((PluginData) that).path) && Objects.equals(this.id, ((PluginData) that).id);
  }
}
