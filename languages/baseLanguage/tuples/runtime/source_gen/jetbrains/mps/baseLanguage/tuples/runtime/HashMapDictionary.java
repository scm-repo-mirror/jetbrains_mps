package jetbrains.mps.baseLanguage.tuples.runtime;

/*Generated by MPS */

import java.util.Map;
import java.util.HashMap;
import java.util.NoSuchElementException;

public abstract class HashMapDictionary implements Dictionary {

  private Map<Dictionary.Key<?>, Object> myStorage = new HashMap<>();

  @Override
  public boolean hasKey(Dictionary.Key<?> key) {
    return myStorage.containsKey(key);
  }

  @Override
  public <T> T getValue(Dictionary.Key<T> key) {
    if (!(myStorage.containsKey(key))) {
      throw new NoSuchElementException();
    }
    return key.valueOf(myStorage.get(key));
  }

  @Override
  public <T> void setValue(Dictionary.Key<T> key, T value) {
    myStorage.put(key, value);
  }

}
