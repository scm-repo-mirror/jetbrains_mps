package jetbrains.mps.lang.text.editor;

/*Generated by MPS */

import jetbrains.mps.openapi.editor.EditorContext;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;
import java.util.List;
import org.jetbrains.mps.openapi.language.SAbstractConcept;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import org.jetbrains.mps.openapi.language.SConcept;
import jetbrains.mps.smodel.adapter.structure.MetaAdapterFactory;

/*package*/ abstract class TextStrategy {
  protected EditorContext myEditorContext;

  /*package*/ TextStrategy(EditorContext editorContext) {
    myEditorContext = editorContext;
  }

  /*package*/ abstract void execute();

  /**
   * Finds the closest ancestor (inclusive) that is a member of a collection
   * 
   * @param currentLine The Line for which the container should be found
   * @return This or the closest ancestor in a multiple cardinality containment link
   */
  public static SNode findLineContainer(SNode currentLine) {
    SNode lineContainer = currentLine;
    while (lineContainer != null && !(SNodeOperations.getContainingLink(lineContainer).isMultiple())) {
      lineContainer = SNodeOperations.getParent(lineContainer);
    }
    if (lineContainer == null) {
      throw new IllegalStateException("The line " + currentLine + " cannot be split. It is not a member of any node collection.");
    }
    return lineContainer;
  }

  /**
   * Retrieves the actual Line from a line container
   * 
   * @param lineContainer The closest ancestor (inclusive) of this that is a member of a collection
   * @return The Line contained in the container, can be null
   */
  public static SNode findLineInContainer(SNode lineContainer) {
    List<SNode> lines = SNodeOperations.getNodeDescendants(lineContainer, CONCEPTS.Line$w3, true, new SAbstractConcept[]{});
    //  
    assert ListSequence.fromList(lines).count() <= 2;
    return ListSequence.fromList(lines).first();
  }

  /**
   * Creates a new line container on the appropriate level of containment as a clone, then replaces the actual Line
   * in that clone with the desired new line
   * 
   * @param lineContainer The closest ancestor (inclusive) of this that is a member of a collection
   * @param newLine The line that should be added as the next Line following this Line
   */
  public static void createNewLineContainer(SNode lineContainer, SNode newLine) {
    SNode clone = SNodeOperations.copyNode(lineContainer);
    SNodeOperations.insertNextSiblingChild(lineContainer, clone);
    SNode lineToReplace = findLineInContainer(clone);
    SNodeOperations.replaceWithAnother(lineToReplace, newLine);
  }


  private static final class CONCEPTS {
    /*package*/ static final SConcept Line$w3 = MetaAdapterFactory.getConcept(0xc7fb639fbe784307L, 0x89b0b5959c3fa8c8L, 0x2331694e561af166L, "jetbrains.mps.lang.text.structure.Line");
  }
}
