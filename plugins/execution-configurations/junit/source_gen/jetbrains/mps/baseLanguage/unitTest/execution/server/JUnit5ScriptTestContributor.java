package jetbrains.mps.baseLanguage.unitTest.execution.server;

/*Generated by MPS */

import org.junit.platform.engine.DiscoverySelector;
import jetbrains.mps.baselanguage.unitTest.execution.launcher.JUnit5TestContributor;
import jetbrains.mps.tool.environment.Environment;
import java.util.Collection;
import jetbrains.mps.baselanguage.unitTest.execution.launcher.ExecutorScript;
import org.jetbrains.annotations.NotNull;
import java.util.List;
import jetbrains.mps.classloading.MPSModuleClassLoader;
import org.junit.platform.engine.discovery.DiscoverySelectors;

/**
 * Adaptation of {@code ScriptTestContributor} to JUnit5 API
 */
public class JUnit5ScriptTestContributor extends AbstractTestConverter<DiscoverySelector> implements JUnit5TestContributor {
  private final Environment myEnv;
  private final Collection<ExecutorScript.TestRecord> myTests;

  public JUnit5ScriptTestContributor(@NotNull Environment env, @NotNull Collection<ExecutorScript.TestRecord> tests) {
    myEnv = env;
    myTests = tests;
  }

  @Override
  public List<DiscoverySelector> collectSelectors() {
    return transfigure(myEnv, myTests);
  }

  @NotNull
  @Override
  protected DiscoverySelector processTestCase(ClassLoader cl, String fqName) throws Exception {
    Class<?> testClass = (cl instanceof MPSModuleClassLoader ? ((MPSModuleClassLoader) cl).loadOwnClass(fqName) : cl.loadClass(fqName));
    return DiscoverySelectors.selectClass(testClass);
  }

  @NotNull
  @Override
  protected DiscoverySelector processTestMethod(ClassLoader cl, String testFqName, String methodName) throws Exception {
    Class<?> testClass = (cl instanceof MPSModuleClassLoader ? ((MPSModuleClassLoader) cl).loadOwnClass(testFqName) : cl.loadClass(testFqName));
    return DiscoverySelectors.selectMethod(testClass, methodName);
  }

  @Override
  protected DiscoverySelector createFailedRequestForClass(String fqName, Exception e) {
    // FIXME implement using special TestEngine
    throw new UnsupportedOperationException("not implemented");
  }

  @Override
  protected DiscoverySelector createFailedRequestForMethod(String testFqName, String methodName, Exception e) {
    // FIXME implement using special TestEngine
    throw new UnsupportedOperationException("not implemented");
  }
}
