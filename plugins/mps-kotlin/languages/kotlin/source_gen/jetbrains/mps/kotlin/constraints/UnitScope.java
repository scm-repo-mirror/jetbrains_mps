package jetbrains.mps.kotlin.constraints;

/*Generated by MPS */

import jetbrains.mps.scope.Scope;
import org.jetbrains.mps.openapi.module.SRepository;
import org.jetbrains.mps.openapi.model.SNode;
import org.jetbrains.annotations.Nullable;
import jetbrains.mps.internal.collections.runtime.Sequence;
import jetbrains.mps.kotlin.api.builtins.BuiltIn;
import java.util.Collections;
import org.jetbrains.annotations.NotNull;
import java.util.Objects;

public class UnitScope extends Scope {
  private static final String UNIT_NAME = "Unit";

  private final SRepository myRepository;
  public UnitScope(SRepository repository) {
    myRepository = repository;
  }

  @Override
  public Iterable<SNode> getAvailableElements(@Nullable String prefix) {
    if ((prefix == null || prefix.length() == 0) || UNIT_NAME.startsWith(prefix)) {
      return Sequence.<SNode>singleton(BuiltIn.UNIT.getClass(myRepository));
    }
    return Sequence.fromIterable(Collections.<SNode>emptyList());
  }
  @Nullable
  @Override
  public SNode resolve(SNode contextNode, @NotNull String refText) {
    if (Objects.equals(refText, UNIT_NAME)) {
      return BuiltIn.UNIT.getClass(contextNode);
    }
    return null;
  }
  @Nullable
  @Override
  public String getReferenceText(SNode contextNode, @NotNull SNode node) {
    if ((node == null)) {
      return null;
    }
    return UNIT_NAME;
  }
}
