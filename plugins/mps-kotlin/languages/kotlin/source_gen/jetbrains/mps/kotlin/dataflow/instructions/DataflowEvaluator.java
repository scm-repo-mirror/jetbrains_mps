package jetbrains.mps.kotlin.dataflow.instructions;

/*Generated by MPS */

import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Nullable;
import java.util.stream.Stream;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.lang.dataFlow.framework.instructions.AbstractJumpInstruction;

@ApiStatus.Experimental
public interface DataflowEvaluator<T> {
  /**
   * Merge input data in a single result. Order of (non nullable) data should not matter (null can only be passed as a
   * second argument if the data type is not nullable).
   */
  T merge(T left, @Nullable T right);

  Stream<State<T>> acceptEquals(SNode left, SNode right, boolean negation, int instructionIndex, State<T> previousState);
  Stream<State<T>> acceptAssign(SNode left, SNode right, boolean isLeftAssignable, int instructionIndex, State<T> previousState);
  Stream<State<T>> acceptIsType(SNode left, SNode right, boolean negation, int instructionIndex, State<T> previousState);
  Stream<State<T>> acceptBackedge(AbstractJumpInstruction instruction, State<T> previousState);

  static Stream<BooleanState> booleanStates() {
    return Stream.of(BooleanState.TRUE, BooleanState.FALSE);
  }
}
