package jetbrains.mps.kotlin.editor;

/*Generated by MPS */

import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.openapi.editor.menus.style.EditorMenuItemStyle;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;

/**
 * Priorities for kotlin code completion
 */
public class CompletionPriorities {
  public static final int TYPE = 40;
  public static final int UNKNOWN = 42;
  public static final int FUNCTION = 44;
  public static final int VARIABLE = 46;

  private static final int EXTERNAL_MODEL_DECAY = 30;
  private static final int EXTERNAL_ROOT_DECAY = 20;
  private static final int UNKNOWN_DECAY = 10;

  public static void apply(int basePriority, Object parameterObject, SNode contextNode, EditorMenuItemStyle style) {
    SNode node = (parameterObject instanceof SNode ? ((SNode) parameterObject) : null);

    if ((node == null)) {
      basePriority -= UNKNOWN_DECAY;
    } else if (SNodeOperations.getModel(node) != SNodeOperations.getModel(contextNode)) {
      basePriority -= EXTERNAL_MODEL_DECAY;
    } else if (SNodeOperations.getContainingRoot(node) != SNodeOperations.getContainingRoot(contextNode)) {
      basePriority -= EXTERNAL_ROOT_DECAY;
    } else {
      style.setBold();
    }

    style.setPriority(basePriority);
  }
}
