package jetbrains.mps.kotlin.scopes.signed;

/*Generated by MPS */

import jetbrains.mps.kotlin.signatures.MemberSignature;
import jetbrains.mps.kotlin.api.members.SourcedSignature;
import org.jetbrains.mps.openapi.model.SNode;

public abstract class DelegatedSignatureScope<T extends MemberSignature> implements SignatureScope {
  private SignatureScope delegated = null;
  /**
   * Create scope, will be called only once and the result will be cached.
   */
  protected abstract SignatureScope buildScope();

  public SignatureScope getDelegated() {
    if (delegated == null) {
      delegated = buildScope();
    }
    return delegated;
  }
  @Override
  public Iterable<SourcedSignature> getElements(String prefix) {
    return getDelegated().getElements(prefix);
  }
  @Override
  public SignatureScope.ContainmentStatus contains(SourcedSignature declaration) {
    return getDelegated().contains(declaration);
  }
  @Override
  public boolean contains(SNode source) {
    return getDelegated().contains(source);
  }
}
