package jetbrains.mps.kotlin.scopes.signed;

/*Generated by MPS */

import jetbrains.mps.kotlin.api.members.SourcedSignature;
import jetbrains.mps.internal.collections.runtime.Sequence;
import jetbrains.mps.internal.collections.runtime.IWhereFilter;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.kotlin.signatures.MemberSignature;

public abstract class FilteringSignatureScope implements SignatureScope {
  private final SignatureScope myNested;
  public FilteringSignatureScope(SignatureScope nested) {
    myNested = nested;
  }

  @Override
  public Iterable<SourcedSignature> getElements(String prefix) {
    return Sequence.fromIterable(myNested.getElements(prefix)).where(new IWhereFilter<SourcedSignature>() {
      public boolean accept(SourcedSignature it) {
        return !(isExcluded(it.getSource())) && !(isExcluded(it.getSignature()));
      }
    });
  }

  @Override
  public SignatureScope.ContainmentStatus contains(SourcedSignature declaration) {
    if (isExcluded(declaration.getSignature()) || isExcluded(declaration.getSource())) {
      return SignatureScope.ContainmentStatus.NO;
    }
    return myNested.contains(declaration);
  }

  @Override
  public boolean contains(SNode source) {
    if (isExcluded(source)) {
      return false;
    }
    return myNested.contains(source);
  }

  public abstract boolean isExcluded(MemberSignature signature);
  public abstract boolean isExcluded(SNode source);
}
