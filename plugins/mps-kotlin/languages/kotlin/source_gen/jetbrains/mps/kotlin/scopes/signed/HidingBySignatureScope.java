package jetbrains.mps.kotlin.scopes.signed;

/*Generated by MPS */

import java.util.List;
import jetbrains.mps.kotlin.api.members.SourcedSignature;
import jetbrains.mps.internal.collections.runtime.Sequence;
import jetbrains.mps.internal.collections.runtime.NotNullWhereFilter;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class HidingBySignatureScope extends CompositeSignatureScope {
  protected HidingBySignatureScope(List<? extends SignatureScope> scopes) {
    super(scopes);
  }

  @Override
  public Iterable<SourcedSignature> getElements(String prefix) {
    return Sequence.fromIterable(super.getElements(prefix)).distinct();
  }

  public static SignatureScope of(SignatureScope... scopes) {
    return of(Sequence.fromArray(scopes));
  }

  public static SignatureScope of(Iterable<SignatureScope> scopes) {
    List<SignatureScope> list = Sequence.fromIterable(scopes).where(new NotNullWhereFilter<SignatureScope>()).toListSequence();
    if (ListSequence.fromList(list).isEmpty()) {
      return null;
    } else {
      // Unlike composite, we still apply for one scope as distinct may not be applied
      return new CompositeSignatureScope(list);
    }
  }
}
