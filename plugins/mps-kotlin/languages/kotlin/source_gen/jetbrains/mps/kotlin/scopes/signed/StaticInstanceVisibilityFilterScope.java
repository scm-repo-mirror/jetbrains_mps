package jetbrains.mps.kotlin.scopes.signed;

/*Generated by MPS */

import jetbrains.mps.kotlin.scopes.VisibilityAccess;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.kotlin.api.members.SourcedSignature;
import jetbrains.mps.kotlin.api.members.SignatureAttributeKey;
import jetbrains.mps.kotlin.behavior.VisibilityModifier__BehaviorDescriptor;

/**
 * Allow to filter static members (IType.getStaticScope result) that are NOT linked to any type wrt to visibility.
 * 
 * These members have a special filtering: they are considered instance members, yet are not handled by getInstanceScopes.
 * Since toplevel filtering does not apply here (we are on a type), we must filter based on a "type" visibility context
 * (which is what this customized filter does)
 * 
 * @see jetbrains.mps.kotlin.scopes.TypeMembersVisitor handles visibility filtering for instance members (applies to companion members as well)
 * @see jetbrains.mps.kotlin.scopes.signed.TopLevelVisibilityFilterScope handles filtering of members detached of any types
 */
public class StaticInstanceVisibilityFilterScope extends TopLevelVisibilityFilterScope {
  private final VisibilityAccess access;
  protected StaticInstanceVisibilityFilterScope(@NotNull SignatureScope scope, SNode contextNode, VisibilityAccess access) {
    super(scope, contextNode);
    this.access = access;
  }

  @Override
  protected boolean isVisibilityApplicable(SourcedSignature signature) {
    SNode visibility = signature.getAttribute(SignatureAttributeKey.VISIBILITY);
    // Note: unlike regular instance members to which TYPE_PROTECTED may be applied (which removes private members from supertypes), these members are not attached to companion type, so they cannot be taken from supertype (private is allowed)
    return visibility == null || (boolean) VisibilityModifier__BehaviorDescriptor.isApplicable_id6jE_6duQ0AR.invoke(visibility, contextNode, access);
  }

  public static SignatureScope of(SignatureScope scope, SNode contextNode, VisibilityAccess access) {
    if (scope == null) {
      return null;
    }
    return new StaticInstanceVisibilityFilterScope(scope, contextNode, access);
  }
}
