package jetbrains.mps.kotlin.smartcast;

/*Generated by MPS */

import jetbrains.mps.kotlin.behavior.TypeReference;
import java.util.Set;
import org.jetbrains.mps.openapi.model.SNode;
import java.util.stream.Collectors;
import java.util.Objects;

public abstract class AggregatedTypeReference implements TypeReference {
  protected final Set<TypeReference> ofTypes;

  protected AggregatedTypeReference(Set<TypeReference> ofTypes) {
    this.ofTypes = ofTypes;
  }

  @Override
  public SNode compute() {
    throw new IllegalStateException("not meant to be used outside typesystem");
  }

  public Set<TypeReference> getNestedTypes() {
    return this.ofTypes;
  }

  @Override
  public String toString() {
    return "(" + ofTypes.stream().map(TypeReference::toString).collect(Collectors.joining(", ")) + ")";
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    AggregatedTypeReference reference = as_izm5ae_a0a1a9(o, AggregatedTypeReference.class);
    return (reference != null ? reference.getClass() == getClass() && Objects.equals(reference.ofTypes, ofTypes) : false);
  }

  @Override
  public int hashCode() {
    // Not hashing with ofType hashcode directly
    return getClass().hashCode() ^ ofTypes.size();
  }
  private static <T> T as_izm5ae_a0a1a9(Object o, Class<T> type) {
    return (type.isInstance(o) ? (T) o : null);
  }
}
