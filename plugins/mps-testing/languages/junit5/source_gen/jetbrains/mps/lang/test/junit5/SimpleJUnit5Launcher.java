package jetbrains.mps.lang.test.junit5;

/*Generated by MPS */

import java.util.Collection;
import java.io.File;

public class SimpleJUnit5Launcher extends AbstractJUnit5Launcher {

  private final Collection<Class<?>> myTestClasses;

  public SimpleJUnit5Launcher(Collection<Class<?>> testClasses) {
    myTestClasses = testClasses;
  }

  @Override
  public int launchTests() {
    FailureDetector failureDetector = new FailureDetector();
    // perhaps, launchTestsWithContextCL, but as long as this code is unused, no idea if we need context CL or not
    launchTests(buildRequest(discover(myTestClasses)), failureDetector);
    return failureDetector.failuresCount();
  }

  @Override
  protected File getTestReportsDir() {
    return new File(System.getProperty("user.dir"));
  }

}
