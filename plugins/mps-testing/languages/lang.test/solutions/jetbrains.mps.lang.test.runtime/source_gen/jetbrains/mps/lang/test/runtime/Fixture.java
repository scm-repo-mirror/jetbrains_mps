package jetbrains.mps.lang.test.runtime;

/*Generated by MPS */

import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.mps.openapi.model.SModel;
import org.jetbrains.mps.openapi.model.SNode;

/**
 * An abstraction of a test fixture that is created for tests operating on copy of MPS model in a sandbox environment.
 * <p>
 * A fixture implementation should provide:
 * <ul>
 *   <li>the SModel instance corresponding to a copy of original smodel</li>
 *   <li>lookup mechanism for a node in the original test model</li>
 *   <li>search for a copied root node with given id</li>
 * </ul>
 */
@ApiStatus.Experimental
public interface Fixture {

  /**
   * Returns the sandbox model created for this fixture.
   */
  SModel model();

  /**
   * Looks up and returns a copied root with given ID.
   * Must be invoked from read action.
   */
  SNode copied(String id);

  /**
   * Looks up and returns an SNode instance in the original model with given ID.
   * Must be invoked from read action.
   */
  SNode original(String id);

}
