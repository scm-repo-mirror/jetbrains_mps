package tooltips.runtime;

/*Generated by MPS */

import javax.swing.JComponent;
import jetbrains.mps.nodeEditor.cells.EditorCell;
import org.jetbrains.annotations.NotNull;
import java.awt.Dimension;
import java.awt.Graphics;
import java.awt.Graphics2D;
import jetbrains.mps.nodeEditor.EditorComponent;

public class TooltipComponent extends JComponent {
  private static final int X_PADDING = 4;
  private static final int Y_PADDING = 2;

  private final EditorCell myTooltipCell;

  public TooltipComponent(@NotNull final jetbrains.mps.openapi.editor.cells.EditorCell tooltipCell) {
    myTooltipCell = (EditorCell) tooltipCell;
  }

  @Override
  public void doLayout() {
    myTooltipCell.relayout();
    setPreferredSize(new Dimension(myTooltipCell.getWidth() + X_PADDING * 2, myTooltipCell.getHeight() + Y_PADDING * 2));
  }

  @Override
  protected void paintComponent(Graphics graphics) {
    Graphics2D graphicsCopy = (Graphics2D) graphics.create();
    try {
      if (!(myTooltipCell.getEditorComponent().isDisposed())) {
        EditorComponent.turnOnAliasingIfPossible(graphicsCopy);
        graphicsCopy.translate(-myTooltipCell.getX() + X_PADDING, -myTooltipCell.getY() + Y_PADDING);
        myTooltipCell.paint(graphicsCopy);
      }
    } finally {
      graphicsCopy.dispose();
    }
  }
}
