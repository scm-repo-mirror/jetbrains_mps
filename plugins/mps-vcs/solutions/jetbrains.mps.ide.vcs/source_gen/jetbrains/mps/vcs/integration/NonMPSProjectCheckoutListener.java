package jetbrains.mps.vcs.integration;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import com.intellij.openapi.vcs.checkout.CheckoutListener;
import org.jetbrains.annotations.NotNull;
import com.intellij.openapi.project.Project;
import java.nio.file.Path;
import jetbrains.mps.workbench.actions.OpenMPSProjectTrustProjectHelper;
import com.intellij.ide.impl.ProjectUtil;
import com.intellij.ide.impl.OpenProjectTask;

@GeneratedClass(nodeId = "3282928695511907695", model = "r:eec25685-8f1e-47c9-a9de-4a7ef6b504ec(jetbrains.mps.vcs.integration)")
public class NonMPSProjectCheckoutListener implements CheckoutListener {
  @Override
  public boolean processCheckedOutDirectory(@NotNull Project project, @NotNull Path directory) {
    // This is not an MPS project, skip all subsequently registered checkout listeners and mimic the  PlatformProjectCheckoutListener here.
    if (OpenMPSProjectTrustProjectHelper.checkTrust(directory)) {
      ProjectUtil.openOrImport(directory, OpenProjectTask.build().withProjectToClose(project));
    }

    return true;
  }
}
