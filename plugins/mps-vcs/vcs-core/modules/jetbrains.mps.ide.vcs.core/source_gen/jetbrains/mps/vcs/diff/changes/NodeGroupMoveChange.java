package jetbrains.mps.vcs.diff.changes;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import org.jetbrains.mps.openapi.language.SContainmentLink;
import jetbrains.mps.vcs.diff.ChangeSet;
import java.util.Objects;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import jetbrains.mps.internal.collections.runtime.IWhereFilter;
import jetbrains.mps.util.NameUtil;
import org.jetbrains.annotations.NotNull;

@GeneratedClass(node = "r:9b4a89e1-ec38-42c4-b1bd-96ab47ffcb3f(jetbrains.mps.vcs.diff.changes)/6640456928090727186", model = "r:9b4a89e1-ec38-42c4-b1bd-96ab47ffcb3f(jetbrains.mps.vcs.diff.changes)")
public class NodeGroupMoveChange extends AbstractNodeGroupChange {

  private final SContainmentLink myOldLink;
  private final SContainmentLink myNewLink;
  private String myDescription;

  public NodeGroupMoveChange(ChangeSet changeSet, ModifiedNodesGroup oldGroup, ModifiedNodesGroup newGroup, SContainmentLink oldLink, SContainmentLink newLink) {
    super(changeSet, oldGroup, newGroup);
    myOldLink = oldLink;
    myNewLink = newLink;
    assert getGroup(false).isNotEmpty() && getGroup(true).isNotEmpty();
    assert Objects.equals(getIds(false), getIds(true));
    assert ListSequence.fromList(getModifiedNodes(false)).all(new IWhereFilter<ModifiedNode>() {
      public boolean accept(ModifiedNode it) {
        return Objects.equals(it.getLink(), myOldLink);
      }
    });
    assert ListSequence.fromList(getModifiedNodes(true)).all(new IWhereFilter<ModifiedNode>() {
      public boolean accept(ModifiedNode it) {
        return Objects.equals(it.getLink(), myNewLink);
      }
    });
    myDescription = createDescription();
  }

  public boolean isUnordered() {
    if (!(Objects.equals(getParentId(false), getParentId(true))) || !(Objects.equals(myOldLink, myNewLink))) {
      return false;
    }
    return myOldLink.isUnordered();
  }

  @Override
  public String toString() {
    return myDescription;
  }

  private String createDescription() {
    String ids = null;
    boolean verbose = true;
    if (verbose) {
      ids = getMultiLineIdsString(false);
    }
    String role = myOldLink.getName();
    String movedStuff = NameUtil.formatNumericalString(ListSequence.fromList(getModifiedNodes(false)).count(), role);
    if (verbose) {
      return String.format("Moved %s: %s", movedStuff, ids);
    } else {
      return String.format("Moved %s", movedStuff);
    }
  }

  @Override
  public String getDescription() {
    return myDescription;
  }

  @NotNull
  @Override
  public ChangeType getType() {
    return ChangeType.MOVE;
  }

  @NotNull
  @Override
  protected ModelChange createOppositeChange() {
    NodeGroupMoveChange oppositeChange = new NodeGroupMoveChange(getChangeSet().getOppositeChangeSet(), getGroup(true), getGroup(false), myNewLink, myOldLink);
    return oppositeChange;
  }
}
