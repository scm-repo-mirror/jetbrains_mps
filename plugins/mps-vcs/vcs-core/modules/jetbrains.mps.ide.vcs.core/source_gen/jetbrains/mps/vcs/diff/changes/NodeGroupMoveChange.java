package jetbrains.mps.vcs.diff.changes;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.vcs.diff.ChangeSet;
import java.util.Objects;
import jetbrains.mps.util.NameUtil;
import org.jetbrains.mps.openapi.model.SModel;
import java.util.List;
import org.jetbrains.mps.openapi.model.SNode;

@GeneratedClass(node = "r:9b4a89e1-ec38-42c4-b1bd-96ab47ffcb3f(jetbrains.mps.vcs.diff.changes)/6640456928090727186", model = "r:9b4a89e1-ec38-42c4-b1bd-96ab47ffcb3f(jetbrains.mps.vcs.diff.changes)")
public final class NodeGroupMoveChange extends HierarchicalNodeGroupChange {

  private final String myDescription;
  private final String myShortDescription;


  public NodeGroupMoveChange(@NotNull ChangeSet changeSet, @NotNull ModifiedNodesGroup oldGroup, @NotNull ModifiedNodesGroup newGroup) {
    super(changeSet, oldGroup, newGroup);
    assert oldGroup.isMove() && newGroup.isMove();
    assert oldGroup.getOppositeMove() == newGroup;
    assert newGroup.getOppositeMove() == oldGroup;
    myDescription = createDescription(true);
    myShortDescription = createDescription(false);
  }

  public boolean isUnordered() {
    return Objects.equals(getParentId(false), getParentId(true)) && Objects.equals(getLink(false), getLink(true)) && getLink(false).isUnordered();
  }

  @Override
  public String toString() {
    return myDescription;
  }

  private String createDescription(boolean verbose) {
    String ids = null;
    if (verbose) {
      ids = getMultiLineIdsString(false);
    }
    String oldRole = getLink(false).getName();
    String oldMovedElements = NameUtil.formatNumericalString(getGroup(false).getSize(), oldRole);
    if (verbose) {
      return String.format("Moved %s: %s", oldMovedElements, ids);
    } else {
      return String.format("Moved %s", oldMovedElements);
    }
  }

  @Override
  public String getDescription() {
    return myDescription;
  }

  @Override
  public String getShortDescription() {
    return myShortDescription;
  }

  @Override
  public void apply(@NotNull SModel model, @NotNull NodeCopier nodeCopier) {
    if (this.getGroup(false).isApplied(model) || this.getGroup(true).isApplied(model)) {
      return;
    }
    List<SNode> nodes = getGroup(false).extractNodes(model, nodeCopier);
    getGroup(false).deleteFromModel(model);
    getGroup(true).insertNodes(nodes, model, nodeCopier);
    this.getGroup(false).setIsApplied(model);
    this.getGroup(true).setIsApplied(model);
  }

  @NotNull
  @Override
  public ChangeType getType() {
    return ChangeType.MOVE;
  }

  @NotNull
  @Override
  protected ModelChange createOppositeChange() {
    NodeGroupMoveChange oppositeChange = new NodeGroupMoveChange(getChangeSet().getOppositeChangeSet(), getGroup(true), getGroup(false));
    return oppositeChange;
  }
}
