package jetbrains.mps.vcs.diff.ui;

/*Generated by MPS */

import com.intellij.history.core.revisions.Revision;
import com.intellij.diff.contents.DiffContent;
import org.jetbrains.annotations.Nullable;
import com.intellij.diff.contents.EmptyContent;
import com.intellij.history.core.tree.Entry;
import org.jetbrains.mps.openapi.model.SModel;
import jetbrains.mps.vcspersistence.VCSPersistenceUtil;
import jetbrains.mps.project.MPSExtentions;
import jetbrains.mps.vcs.platform.integration.ModelDiffContent;

/*package*/ final class RevisionImpl extends Revision {
  private final String myName;
  private final long myTimestamp;
  private byte[] myContentBytes;
  private DiffContent myContent;

  /*package*/ RevisionImpl(String name, long timestamp, @Nullable byte[] content) {
    myName = name;
    myTimestamp = timestamp;
    myContentBytes = content;
    if (content == null) {
      myContent = new EmptyContent();
    }
  }


  @Nullable
  @Override
  public String getChangeSetName() {
    return myName;
  }

  @Override
  public long getTimestamp() {
    return myTimestamp;
  }

  @Override
  public Entry findEntry() {
    return null;
  }

  /*package*/ DiffContent getContent() {
    if (myContent != null) {
      return myContent;
    }
    SModel loaded = VCSPersistenceUtil.loadModel(myContentBytes, MPSExtentions.MODEL);
    // ModelDiffViewer doesn't tolerate reusable detached models, it registers and disposes such models solely on its own discretion 
    return (loaded == null ? new EmptyContent() : new ModelDiffContent(loaded));
  }
}
