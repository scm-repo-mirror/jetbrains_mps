package jetbrains.mps.vcs.diff.ui.common;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import org.jetbrains.annotations.NotNull;
import java.util.List;
import java.awt.event.MouseEvent;
import java.awt.Point;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import jetbrains.mps.internal.collections.runtime.IWhereFilter;
import jetbrains.mps.internal.collections.runtime.IterableUtils;
import jetbrains.mps.internal.collections.runtime.ISelector;
import java.util.Objects;
import jetbrains.mps.internal.collections.runtime.IVisitor;

@GeneratedClass(node = "r:07568eb8-30c0-4bb3-9dcb-50ee4b8de59a(jetbrains.mps.vcs.diff.ui.common)/2292925610559614969", model = "r:07568eb8-30c0-4bb3-9dcb-50ee4b8de59a(jetbrains.mps.vcs.diff.ui.common)")
/*package*/ final class DiffEditorLayersHolder {

  @NotNull
  private final List<DiffEditorChangeLayer> myLayers;


  /*package*/ DiffEditorLayersHolder(@NotNull List<DiffEditorChangeLayer> layers) {
    myLayers = layers;
  }

  @NotNull
  /*package*/ List<DiffEditorChangeLayer> getLayersUnderMouse(MouseEvent e) {
    final Point p = e.getPoint();
    return ListSequence.fromList(ListSequence.fromList(myLayers).where(new IWhereFilter<DiffEditorChangeLayer>() {
      public boolean accept(DiffEditorChangeLayer layer) {
        return p.y >= layer.getY() && p.y <= layer.getY() + layer.getHeight();
      }
    }).toListSequence()).reversedList();
  }

  /*package*/ String getToolTipTextFromSelectedLayers() {
    return IterableUtils.join(ListSequence.fromList(myLayers).where(new IWhereFilter<DiffEditorChangeLayer>() {
      public boolean accept(DiffEditorChangeLayer it) {
        return it.isSelected();
      }
    }).select(new ISelector<DiffEditorChangeLayer, String>() {
      public String select(DiffEditorChangeLayer it) {
        return it.getDescription().replace("\n", "<br>");
      }
    }), "<br><br>");
  }

  @NotNull
  /*package*/ List<DiffEditorChangeLayer> getLayers() {
    return myLayers;
  }

  /*package*/ void buildLayersHierarchy() {
    for (int i = 0; i < ListSequence.fromList(myLayers).count(); i++) {
      DiffEditorChangeLayer l1 = ListSequence.fromList(myLayers).getElement(i);
      Bounds b1 = l1.getGroupBounds();
      for (int j = i + 1; j < ListSequence.fromList(myLayers).count(); j++) {
        DiffEditorChangeLayer l2 = ListSequence.fromList(myLayers).getElement(j);
        Bounds b2 = l2.getGroupBounds();
        if (!(Objects.equals(b1, b2))) {
          if (b2.contains(b1)) {
            l2.addChild(l1);
          }
          if (b1.contains(b2)) {
            l1.addChild(l2);
          }
        }
      }
    }
    ListSequence.fromList(myLayers).where(new IWhereFilter<DiffEditorChangeLayer>() {
      public boolean accept(DiffEditorChangeLayer it) {
        return !(it.hasParent());
      }
    }).visitAll(new IVisitor<DiffEditorChangeLayer>() {
      public void visit(DiffEditorChangeLayer it) {
        it.setRankAndParent(0, null);
      }
    });
  }

  /*package*/ void sortLayers() {
    List<DiffEditorChangeLayer> sortedLayers = ListSequence.fromList(myLayers).sort(new ISelector<DiffEditorChangeLayer, Integer>() {
      public Integer select(DiffEditorChangeLayer it) {
        return it.getRank();
      }
    }, true).alsoSort(new ISelector<DiffEditorChangeLayer, Integer>() {
      public Integer select(DiffEditorChangeLayer it) {
        return getNeighbourRank(it, false);
      }
    }, true).alsoSort(new ISelector<DiffEditorChangeLayer, Integer>() {
      public Integer select(DiffEditorChangeLayer it) {
        return getNeighbourRank(it, true);
      }
    }, true).toListSequence();
    ListSequence.fromList(myLayers).clear();
    ListSequence.fromList(myLayers).addSequence(ListSequence.fromList(sortedLayers));
  }

  private static int getNeighbourRank(DiffEditorChangeLayer layer, boolean right) {
    if (layer.isLeft() != right) {
      return 0;
    }
    DiffEditorChangeLayer oppositeLayer = layer.getOppositeLayer();
    return (oppositeLayer != null ? oppositeLayer.getRank() : 0);
  }

  /*package*/ void calculateLayersCoordinates() {
    ListSequence.fromList(myLayers).sort(new ISelector<DiffEditorChangeLayer, Integer>() {
      public Integer select(DiffEditorChangeLayer it) {
        return it.getGroupBounds()._0();
      }
    }, true).alsoSort(new ISelector<DiffEditorChangeLayer, Integer>() {
      public Integer select(DiffEditorChangeLayer it) {
        return it.getGroupBounds()._1();
      }
    }, true).visitAll(new IVisitor<DiffEditorChangeLayer>() {
      public void visit(final DiffEditorChangeLayer layer) {
        layer.calculateCoordinates(ListSequence.fromList(myLayers).where(new IWhereFilter<DiffEditorChangeLayer>() {
          public boolean accept(DiffEditorChangeLayer it) {
            return it != layer;
          }
        }));
      }
    });
  }
}
