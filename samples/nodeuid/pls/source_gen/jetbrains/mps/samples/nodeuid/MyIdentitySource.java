package jetbrains.mps.samples.nodeuid;

/*Generated by MPS */

import jetbrains.mps.smodel.NodeIdentitySupplier;
import org.jetbrains.mps.openapi.model.SNodeId;
import org.jetbrains.mps.openapi.model.SModel;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.mps.openapi.model.SNode;
import org.jetbrains.annotations.Nullable;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;
import java.util.Objects;
import java.util.Map;
import org.jetbrains.mps.openapi.language.SAbstractConcept;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SConceptOperations;
import org.jetbrains.mps.openapi.language.SConcept;
import jetbrains.mps.smodel.adapter.structure.MetaAdapterFactory;

public class MyIdentitySource implements NodeIdentitySupplier {

  @Override
  public SNodeId issue(SModel model) {
    return jetbrains.mps.smodel.SModel.generateUniqueId();
  }
  @Override
  public void configure(@NotNull SNode node, @Nullable SModel model, @Nullable SNode origin) {
    if (!(isOfInterest(SNodeOperations.getConcept(node)))) {
      return;
    }
    // node.uid == null means we've got new node to initialize
    // node.uid != null && origin == null is Cut scenario, where we preserved uid of original node but no information about the origin
    // node.uid != null with the same origin.uid means we're cloning a model/node and need to re-issue UID for a copy
    if (isEmptyString(SNodeOperations.get_uid(node)) || (origin != null && Objects.equals(SNodeOperations.get_uid(origin), SNodeOperations.get_uid(node)))) {
      String newValue = "uid:sample:" + System.nanoTime();
      SNodeOperations.set_uid(node, newValue);
    }
  }
  @Override
  public void moved(@NotNull SNode node, @NotNull Map<SNode, SNode> newNodes) {
    if (!(isOfInterest(SNodeOperations.getConcept(node)))) {
      return;
    }
    for (SNode nn : SNodeOperations.getNodeDescendants(node, CONCEPTS.ExpressionWithId$Np, true, new SAbstractConcept[]{})) {
      SNode moved = newNodes.get(nn);
      String uid = SNodeOperations.get_uid(nn);
      if (moved != null && (uid != null && uid.length() > 0)) {
        SNodeOperations.set_uid(moved, uid);
      }
    }
  }

  private boolean isOfInterest(SAbstractConcept c) {
    return SConceptOperations.isSubConceptOf(SNodeOperations.asSConcept(c), CONCEPTS.ExpressionWithId$Np);
  }
  private static boolean isEmptyString(String str) {
    return str == null || str.isEmpty();
  }

  private static final class CONCEPTS {
    /*package*/ static final SConcept ExpressionWithId$Np = MetaAdapterFactory.getConcept(0xd1577ca1c7c14121L, 0x9ada937bdec4157cL, 0x49a2cceac9614e7dL, "jetbrains.mps.samples.nodeuid.lang.structure.ExpressionWithId");
  }
}
