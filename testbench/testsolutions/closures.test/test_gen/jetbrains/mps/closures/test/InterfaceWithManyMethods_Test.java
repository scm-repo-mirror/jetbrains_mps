package jetbrains.mps.closures.test;

/*Generated by MPS */

import java.io.File;

public class InterfaceWithManyMethods_Test {
  public interface MultipleMethods {
    default void trap(String s) {
      // no-op
    }
    String actualMethod(File file);
    default int secondTrap(double d) {
      return (int) d;
    }
  }
  public abstract class AbstractMultipleMethods {
    public void trap(String s) {
      // no-op
    }
    public abstract String actualMethod(File file);
    public int secondTrap(double d) {
      return (int) d;
    }
  }

  private void run(MultipleMethods methods) {
    // no-op
  }
  private void runAbstract(AbstractMultipleMethods method) {
    // no-op
  }

  private void test() {
    // Some logic seemed to rely on the first method found, this ensures compilation works for those cases
    run((String file) -> file.getName());
    run((String file) -> file.getName());
    runAbstract(new AbstractMultipleMethods() {
      public String actualMethod(File file) {
        return file.getName();
      }
    });
    runAbstract(new AbstractMultipleMethods() {
      public String actualMethod(File file) {
        return file.getName();
      }
    });
  }
}
